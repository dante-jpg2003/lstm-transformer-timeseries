# Motor_Fault_Detection
# Motor Fault Detection

CNN-GRU fusion model for synchronous motor electrical fault classification.

## Setup
1. Clone this repo
2. Create virtual environment: `python -m venv venv`
3. Activate: `venv\Scripts\activate` (Windows)
4. Install dependencies: `pip install -r requirements.txt`

## Dataset
Download from: https://gitlab.com/power-systems-technion/motor-faults/-/tree/main
Place .mat files in `data/raw/`
```


